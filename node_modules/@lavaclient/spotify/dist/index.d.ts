import { Dispatcher } from 'undici';
import { Manager, Dictionary } from 'lavaclient';
import * as Lavalink from '@lavaclient/types/v3';

declare namespace Spotify {
    interface PagingObject<T> {
        href: string;
        items: T[];
        limit: number;
        next: string | null;
        offset: number;
        previous: string;
        total: number;
    }
    interface ExternalIdObject {
        isrc?: string;
        ean?: string;
        upc?: string;
    }
    interface ExternalUrlObject {
        spotify: string;
    }
    interface ContextObject {
        type: "artist" | "playlist" | "album" | "track" | "show" | "episode";
        href: string;
        external_urls: ExternalUrlObject;
        uri: string;
    }
    interface ImageObject {
        height?: number;
        url: string;
        width?: number;
    }
    interface FollowersObject {
        href: null;
        total: number;
    }
    interface RestrictionsObject {
        reason: string;
    }
    interface Album extends ContextObject {
        album_group?: "album" | "single" | "compilation" | "appears_on";
        album_type: "album" | "single" | "compilation";
        artists: Artist[];
        available_markets?: string[];
        id: string;
        images: ImageObject[];
        name: string;
        release_date: string;
        release_date_precision: "year" | "month" | "day";
        restrictions?: RestrictionsObject;
        type: "album";
        copyrights: CopyrightObject[];
        external_ids: ExternalIdObject;
        genres: string[];
        label: string;
        popularity: number;
        tracks: PagingObject<Track>;
    }
    interface CopyrightObject {
        text: string;
        type: "C" | "P";
    }
    interface Artist extends ContextObject {
        name: string;
        id: string;
        type: "artist";
        followers: FollowersObject;
        genres: string[];
        images: ImageObject[];
        popularity: number;
    }
    interface Playlist extends ContextObject {
        collaborative: boolean;
        description: string | null;
        id: string;
        images: ImageObject[];
        name: string;
        owner: User;
        public: boolean | null;
        snapshot_id: string;
        type: "playlist";
        followers: FollowersObject;
        tracks: PagingObject<PlaylistTrack>;
    }
    interface PlaylistTrack {
        added_at: string;
        added_by: User;
        is_local: boolean;
        track: Track | null;
    }
    interface User {
        display_name?: string;
        external_urls: ExternalUrlObject;
        followers?: FollowersObject;
        href: string;
        id: string;
        images?: ImageObject[];
        type: "user";
        uri: string;
    }
    interface Track {
        artists: Artist[];
        available_markets?: string[];
        disc_number: number;
        duration_ms: number;
        explicit: boolean;
        external_urls: ExternalUrlObject;
        href: string;
        id: string;
        is_playable?: boolean;
        linked_from?: TrackLinkObject;
        restrictions?: RestrictionsObject;
        name: string;
        preview_url: string | null;
        track_number: number;
        type: "track";
        uri: string;
        album: Album;
        external_ids: ExternalIdObject;
        popularity: number;
        is_local?: boolean;
    }
    interface TrackLinkObject {
        external_urls: ExternalUrlObject;
        href: string;
        id: string;
        type: "track";
        uri: string;
    }
}

declare class PageLoader {
    readonly baseUrl: string;
    readonly strategy: PageLoadingStrategy;
    readonly fetchPage: PageLoaderOptions["fetchPage"];
    constructor(options: PageLoaderOptions);
    static createPages(trackCount: number, strategy: PageLoadingStrategy): number[];
    load(manager: SpotifyManager, resourceId: string, data: InitialData): Promise<Spotify.Track[]>;
    private createUrl;
}
interface PageLoaderOptions {
    baseUrl: string;
    strategy: PageLoadingStrategy;
    fetchPage: (manager: SpotifyManager, path: string) => Promise<Spotify.Track[]>;
}
interface PageLoadingStrategy {
    type: "parallel" | "sequential";
    limit: number;
    size: number;
}
interface InitialData {
    tracks: Spotify.Track[];
    total: number;
}

declare class SpotifyManager {
    #private;
    static readonly API_URL = "https://api.spotify.com";
    static readonly DEFAULT_PAGE_STRATEGY: PageLoadingStrategy;
    static readonly SOURCE_PREFIX: {
        youtube: string;
        "youtube music": string;
        soundcloud: string;
    };
    static readonly DEFAULTS: Omit<SpotifyManagerOptions, "client">;
    readonly dispatcher: Dispatcher;
    readonly manager: Manager;
    readonly options: Required<SpotifyManagerOptions>;
    loaders: Loader[];
    searchPrefix: string;
    constructor(lavaclient: Manager, options: SpotifyManagerOptions);
    get token(): string | null;
    private get encoded();
    isSpotifyUrl(url: string): boolean;
    makeRequest<T extends Dictionary = Dictionary>(endpoint: string): Promise<T>;
    load(url: string): Promise<Item | null>;
    renew(): Promise<void>;
}
declare type SearchPrefix = "youtube" | "youtube music" | "soundcloud";
interface SpotifyClientOptions {
    id: string;
    secret: string;
}
interface SpotifyManagerOptions {
    pages?: {
        album?: PageLoadingStrategy;
        playlist?: PageLoadingStrategy;
    };
    client: SpotifyClientOptions;
    loaders?: SpotifyItemType[];
    autoResolveYoutubeTracks?: boolean;
    searchPrefix?: SearchPrefix;
    searchFormat?: string;
    market?: string;
}

declare abstract class SpotifyItem {
    readonly manager: SpotifyManager;
    abstract type: SpotifyItemType;
    protected constructor(manager: SpotifyManager);
}
declare enum SpotifyItemType {
    Artist = 0,
    Playlist = 1,
    Track = 2,
    Album = 3
}

declare class SpotifyTrack extends SpotifyItem {
    #private;
    type: SpotifyItemType.Track;
    data: Spotify.Track;
    constructor(manager: SpotifyManager, track: Spotify.Track);
    get name(): string;
    get artists(): Spotify.Artist[];
    get album(): Spotify.Album;
    get artwork(): string;
    resolveYoutubeTrack(): Promise<Lavalink.Track>;
}
declare function createTrackFactory(manager: SpotifyManager): (track: Spotify.Track) => SpotifyTrack;

declare class SpotifyAlbum extends SpotifyItem {
    readonly type: SpotifyItemType.Album;
    data: Spotify.Album;
    readonly tracks: SpotifyTrack[];
    constructor(manager: SpotifyManager, album: Spotify.Album, tracks: Array<SpotifyTrack>);
    get name(): string;
    get artists(): Spotify.Artist[];
    get artwork(): string | null;
    static convertTracks(manager: SpotifyManager, tracks: Spotify.Track[]): SpotifyTrack[];
    resolveYoutubeTracks(): Promise<Lavalink.Track[]>;
}

declare class SpotifyPlaylist extends SpotifyItem {
    readonly type: SpotifyItemType.Playlist;
    readonly data: Spotify.Playlist;
    readonly tracks: SpotifyTrack[];
    constructor(manager: SpotifyManager, album: Spotify.Playlist, tracks: Array<SpotifyTrack>);
    get name(): string;
    get owner(): Spotify.User;
    get artwork(): string;
    resolveYoutubeTracks(): Promise<Lavalink.Track[]>;
}

declare class SpotifyArtist extends SpotifyItem {
    type: SpotifyItemType.Artist;
    readonly data: Spotify.Artist;
    readonly topTracks: SpotifyTrack[];
    constructor(manager: SpotifyManager, data: Spotify.Artist, topTracks: Spotify.Track[]);
    get name(): string;
    get artwork(): string;
    resolveYoutubeTracks(): Promise<Lavalink.Track[]>;
}

declare abstract class Loader {
    abstract readonly itemType: SpotifyItemType;
    abstract readonly matchers: RegExp[];
    abstract load(manager: SpotifyManager, execArray: RegExpExecArray): Promise<Item>;
    match(identifier: string): RegExpExecArray | null;
}
declare type Item = SpotifyTrack | SpotifyAlbum | SpotifyPlaylist | SpotifyArtist;

declare const _manager: unique symbol;
declare function load(options: SpotifyManagerOptions): void;
declare module "lavaclient" {
    interface Node {
        readonly spotify: SpotifyManager;
        [_manager]?: SpotifyManager;
    }
    interface Cluster {
        readonly spotify: SpotifyManager;
        [_manager]?: SpotifyManager;
    }
}

export { InitialData, Item, Loader, PageLoader, PageLoaderOptions, PageLoadingStrategy, SearchPrefix, Spotify, SpotifyAlbum, SpotifyArtist, SpotifyClientOptions, SpotifyItem, SpotifyItemType, SpotifyManager, SpotifyManagerOptions, SpotifyPlaylist, SpotifyTrack, _manager, createTrackFactory, load };
